<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:ErmayMuhasebe.Avalonia.ViewModels;assembly=ErmayMuhasebe.Avalonia"
             xmlns:ic="using:FluentIcons.Avalonia.Fluent"
             xmlns:models="clr-namespace:ErmayMuhasebe.Models;assembly=ErmayMuhasebe.Shared"
             mc:Ignorable="d" d:DesignWidth="1000" d:DesignHeight="600"
             x:Class="ErmayMuhasebe.Avalonia.Views.KasaListView"
             x:DataType="vm:KasaListViewModel"
             x:Name="root">

  <Grid RowDefinitions="Auto, *" Margin="25">
    <!-- Header -->
    <Grid Grid.Row="0" ColumnDefinitions="Auto, *, Auto" Margin="0,0,0,25">
        <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="15">
            <Border Background="{StaticResource FluidBlueGradient}" Width="48" Height="48" CornerRadius="12">
                <ic:SymbolIcon Symbol="Money" Foreground="White" FontSize="24" HorizontalAlignment="Center"/> 
            </Border>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Kasa / Nakit Hesapları" FontSize="20" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="Nakit akışını ve kasa bakiyelerini yönetin" FontSize="12" Foreground="#88FFFFFF"/>
            </StackPanel>
        </StackPanel>

        <Button Grid.Column="2" Command="{Binding OpenAddKasaDialogCommand}" Classes="SideButton Info" Width="180">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="10">
                <ic:SymbolIcon Symbol="Add" FontSize="18"/> 
                <TextBlock Text="Yeni Kasa Tanımla" FontWeight="Bold"/>
            </StackPanel>
        </Button>
    </Grid>

    <!-- Content -->
    <Border Grid.Row="1" Background="{DynamicResource PanelBrush}" CornerRadius="16" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" Padding="10">
      <DataGrid ItemsSource="{Binding Kasalar}" 
                SelectedItem="{Binding SelectedKasa}"
                AutoGenerateColumns="False" IsReadOnly="True"
                GridLinesVisibility="Horizontal" Background="Transparent"
                BorderThickness="0" RowHeight="45"
                DoubleTapped="OnDoubleTapped">
        <DataGrid.Styles>
            <Style Selector="DataGridRow">
                <Setter Property="ContextMenu">
                    <Setter.Value>
                        <ContextMenu x:DataType="models:BankaKart">
                            <MenuItem Header="Detayları Görüntüle" 
                                      Command="{Binding #root.DataContext.NavigateToDetayCommand}" 
                                      CommandParameter="{Binding}">
                                <MenuItem.Icon><ic:SymbolIcon Symbol="History"/></MenuItem.Icon>
                            </MenuItem>
                            <MenuItem Header="Düzenle" 
                                      Command="{Binding #root.DataContext.OpenEditKasaDialogCommand}" 
                                      CommandParameter="{Binding}">
                                <MenuItem.Icon><ic:SymbolIcon Symbol="Edit"/></MenuItem.Icon>
                            </MenuItem>
                            <MenuItem Header="Sil" 
                                      Command="{Binding #root.DataContext.DeleteKasaConfirmCommand}" 
                                      CommandParameter="{Binding}">
                                <MenuItem.Icon><ic:SymbolIcon Symbol="Delete" Foreground="Red"/></MenuItem.Icon>
                            </MenuItem>
                            <Separator/>
                            <MenuItem Header="Ekstre Raporu (PDF)" 
                                      Command="{Binding #root.DataContext.GenerateReportCommand}" 
                                      CommandParameter="{Binding}">
                                <MenuItem.Icon><ic:SymbolIcon Symbol="Print"/></MenuItem.Icon>
                            </MenuItem>
                        </ContextMenu>
                    </Setter.Value>
                </Setter>
            </Style>
        </DataGrid.Styles>
        <DataGrid.Columns>
          <DataGridTextColumn Header="Kasa Adı" Binding="{Binding BankaAdi}" Width="250"/>
          <DataGridTextColumn Header="Döviz" Binding="{Binding DovizTuru}" Width="100"/>
          <DataGridTemplateColumn Header="Bakiye" Width="200">
              <DataTemplate>
                  <TextBlock Text="{Binding GuncelBakiye, StringFormat='N2'}" FontWeight="Bold" Foreground="#2ecc71" VerticalAlignment="Center" Margin="10,0"/>
              </DataTemplate>
          </DataGridTemplateColumn>
          <DataGridTextColumn Header="Yetkili" Binding="{Binding Yetkili}" Width="*"/>
          <DataGridTemplateColumn Header="İşlemler" Width="120">
             <DataTemplate>
               <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center">
                 <Button Command="{Binding #root.DataContext.OpenEditKasaDialogCommand}"
                         CommandParameter="{Binding}" Classes="SideButton Secondary" 
                         Width="38" Height="36" Padding="0">
                     <ic:SymbolIcon Symbol="Edit" FontSize="18" Foreground="White"/> 
                 </Button>
                 <Button Command="{Binding #root.DataContext.DeleteKasaConfirmCommand}" 
                         CommandParameter="{Binding}" Classes="SideButton Danger" 
                         Width="38" Height="36" Padding="0">
                     <ic:SymbolIcon Symbol="Delete" FontSize="18" Foreground="White"/> 
                 </Button>
               </StackPanel>
             </DataTemplate>
          </DataGridTemplateColumn>
        </DataGrid.Columns>
      </DataGrid>
    </Border>

    <Grid Grid.RowSpan="2" IsVisible="{Binding IsEditDialogVisible}">
        <Border Background="#CC000000" IsVisible="{Binding IsEditDialogVisible}"/>
        <Border Background="{DynamicResource PanelBrush}" 
                Width="500" VerticalAlignment="Center" HorizontalAlignment="Center" 
                CornerRadius="24" Padding="0"
                BorderBrush="{DynamicResource BorderColor}" BorderThickness="1"
                BoxShadow="0 10 30 0 #66000000">
            <Grid RowDefinitions="Auto, *, Auto">
                <!-- Header -->
                <Border Grid.Row="0" Background="#11FFFFFF" Padding="25,20" CornerRadius="24,24,0,0">
                    <StackPanel Spacing="5">
                        <TextBlock Text="{Binding DialogTitle}" FontSize="20" FontWeight="Bold" Foreground="White"/>
                        <TextBlock Text="Kasa hesaplarını ve açılış bakiyelerini yönetin" FontSize="12" Opacity="0.5"/>
                    </StackPanel>
                </Border>

                <!-- Form -->
                <Border Grid.Row="1" Padding="30">
                    <StackPanel Spacing="25">
                        <StackPanel Spacing="8">
                            <TextBlock Text="KASA ADI" FontSize="11" FontWeight="Bold" Foreground="{StaticResource SystemAccentColor}" Opacity="0.8" Margin="2,0"/>
                            <TextBox Text="{Binding EditKasaAdi}" 
                                     Watermark="Örn: Merkez Kasa, Mağaza Kasası..."
                                     MinHeight="45" VerticalContentAlignment="Center"/>
                        </StackPanel>
                        
                        <Grid ColumnDefinitions="*, *">
                            <StackPanel Grid.Column="0" Margin="0,0,15,0" Spacing="8">
                                <TextBlock Text="DÖVİZ TÜRÜ" FontSize="11" FontWeight="Bold" Foreground="{StaticResource SystemAccentColor}" Opacity="0.8" Margin="2,0"/>
                                <ComboBox SelectedItem="{Binding EditDovizTuru}" 
                                          HorizontalAlignment="Stretch"
                                          Height="45">
                                    <x:String>TL</x:String>
                                    <x:String>USD</x:String>
                                    <x:String>EUR</x:String>
                                    <x:String>GBP</x:String>
                                </ComboBox>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Spacing="8">
                                <TextBlock Text="AÇILIŞ BAKİYESİ" FontSize="11" FontWeight="Bold" Foreground="{StaticResource SystemAccentColor}" Opacity="0.8" Margin="2,0"/>
                                <NumericUpDown Value="{Binding EditAcilisBakiyesi}" 
                                             VerticalContentAlignment="Center"
                                             HorizontalAlignment="Stretch"
                                             Height="45"
                                             FormatString="N2"/>
                            </StackPanel>
                        </Grid>
                        
                        <StackPanel Spacing="8">
                            <TextBlock Text="YETKİLİ KİŞİ" FontSize="11" FontWeight="Bold" Foreground="{StaticResource SystemAccentColor}" Opacity="0.8" Margin="2,0"/>
                            <TextBox Text="{Binding EditYetkili}" 
                                     Watermark="Kasa sorumlusunun adı..."
                                     MinHeight="45" VerticalContentAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Footer -->
                <Border Grid.Row="2" Background="#08FFFFFF" Padding="25,20" CornerRadius="0,0,24,24">
                    <Grid ColumnDefinitions="*, *">
                        <Button Grid.Column="0" Content="İptal" Command="{Binding CloseDialogCommand}" 
                                HorizontalAlignment="Stretch" Margin="0,0,10,0"
                                Height="45"
                                Classes="SideButton Secondary"/>
                        <Button Grid.Column="1" Content="Kaydet" Command="{Binding SaveKasaCommand}" 
                                HorizontalAlignment="Stretch"
                                Height="45"
                                Classes="SideButton Info"/>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>

    <!-- DELETE CONFIRMATION OVERLAY -->
    <Grid Grid.RowSpan="3" Background="#E6000000" IsVisible="{Binding IsConfirmVisible}">
         <Border Background="#080A0C" BorderBrush="#33FFFFFF" BorderThickness="1" CornerRadius="16" Width="400" VerticalAlignment="Center" HorizontalAlignment="Center" BoxShadow="0 20 60 0 #CC000000" ClipToBounds="True">
             <Grid RowDefinitions="Auto, *">
                  <Border Grid.Row="0" Background="#26FF416C" Padding="20,12">
                      <StackPanel Orientation="Horizontal" Spacing="12">
                          <ic:SymbolIcon Symbol="Delete" Foreground="White" FontSize="20"/> 
                          <TextBlock Text="Onay Gerekiyor" FontWeight="Bold" FontSize="15" Foreground="White" VerticalAlignment="Center"/>
                      </StackPanel>
                  </Border>

                  <StackPanel Grid.Row="1" Margin="30" Spacing="25">
                      <ic:SymbolIcon Symbol="Warning" Foreground="#FF416C" FontSize="48" HorizontalAlignment="Center"/> 
                      <StackPanel Spacing="8">
                          <TextBlock Text="{Binding ConfirmTitle}" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" FontSize="18"/>
                          <TextBlock Text="{Binding ConfirmMessage}" Foreground="#BBB" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap" FontSize="13"/>
                      </StackPanel>
                      
                      <Grid ColumnDefinitions="*,*" Margin="0,10,0,0">
                          <Button Grid.Column="0" Classes="SideButton Danger" Command="{Binding ConfirmYesCommand}" Margin="0,0,5,0" Height="45">
                              <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center">
                                  <ic:SymbolIcon Symbol="Checkmark" FontSize="18" VerticalAlignment="Center"/> 
                                  <TextBlock Text="EVET, SİL" FontWeight="Bold" VerticalAlignment="Center" FontSize="12"/>
                              </StackPanel>
                          </Button>
                          <Button Grid.Column="1" Classes="SideButton Secondary" Command="{Binding ConfirmNoCommand}" Margin="5,0,0,0" Height="45">
                               <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center">
                                  <ic:SymbolIcon Symbol="Dismiss" FontSize="18" VerticalAlignment="Center"/> 
                                  <TextBlock Text="VAZGEÇ" FontWeight="Bold" VerticalAlignment="Center" FontSize="12"/>
                               </StackPanel>
                          </Button>
                      </Grid>
                  </StackPanel>
             </Grid>
         </Border>
    </Grid>
  </Grid>
</UserControl>
